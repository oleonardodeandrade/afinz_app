import 'package:flutter/material.dart';

class TransferScreenPlaceholder extends StatelessWidget {
  const TransferScreenPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Transferir')),
      body: const Center(child: Text('Tela de Transferência')),
    );
  }
}
